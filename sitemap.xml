---
layout: null
sitemap: false
---
{%- comment -%}
  The list of pages offered to a search engine, derived from the site itself so
  it cannot fall out of step. Never maintain this by hand.

  Two exclusions, both deliberate and both visible here rather than hidden in a
  page's front matter:
    - the 404, which should never be indexed
    - anything that sets `sitemap: false`, including this file

  Every address uses absolute_url, so it names crissouza.org and matches that
  page's canonical exactly.
{%- endcomment -%}
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for page in site.pages %}
{%- unless page.sitemap == false or page.url contains '404' or page.url contains '.xml' or page.url contains '.txt' %}
  <url>
    <loc>{{ page.url | absolute_url }}</loc>
  </url>
{%- endunless %}
{%- endfor %}
{%- for post in site.posts %}
  <url>
    <loc>{{ post.url | absolute_url }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
  </url>
{%- endfor %}
</urlset>
